# JWT 认证（替换内存 Token）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用无状态 JWT(HS256) + token 版本号撤销替换内存 TokenManager，使登录态跨进程重启存活，不再每次重启都需重新登录。

**Architecture:** JWT 自身携带 `exp`，服务端只做验签 + 过期判断 + 版本号比对，不持有 token 状态。撤销靠持久化的整数 `token_version`：登出/改密时 +1，使所有旧 token 失效。HS256 密钥与版本号持久化进 `data/homelab-panel.json`（**不 bump `dataVersion`**，懒初始化，绝不触发破坏性重置）。

**Tech Stack:** Go 1.26 / Gin / `github.com/golang-jwt/jwt/v5`；前端 React + axios + zustand（localStorage 存 token，零结构性改动）。

**设计文档：** `docs/superpowers/specs/2026-06-19-jwt-auth-design.md`

---

## 文件结构

| 文件 | 责任 | 动作 |
|---|---|---|
| `internal/data/models.go` | 新增 `Auth` 结构 + `StoreData.Auth` 字段 | 改 |
| `internal/data/store.go` | 廉价访问器：`Secret/TokenVersion/EnsureSecret/IncrementTokenVersion` | 改 |
| `internal/data/store_test.go` | 密钥懒生成与版本号持久化测试 | 改 |
| `internal/handlers/tokens.go` | 重写为 JWT 签发/校验（无状态） | 重写 |
| `internal/handlers/tokens_test.go` | JWT 签发/过期/篡改/版本撤销测试 | 重写 |
| `internal/handlers/handlers.go` | `NewTokenManager` 改为接收 store、TTL 改 30 天 | 改 |
| `internal/handlers/middleware.go` | 移除已无用的 token 上下文辅助代码 | 改 |
| `internal/handlers/auth.go` | 登出 `Revoke()`；改密后 bump 版本并重签 token | 改 |
| `internal/handlers/auth_test.go` | 登录/登出/改密 handler 行为测试 | 新建 |
| `internal/app/app.go` | 启动时 `EnsureSecret` | 改 |
| `web/src/api/admin.ts` | `changePassword` 返回类型加 `token/expires_at` | 改 |
| `go.mod` / `go.sum` | 新增 `golang-jwt/jwt/v5` | 改 |

> 关键约束：`Issue()` 内部会调用 `EnsureSecret()`，因此现有 `panel_test.go` 中 `h.tokens.Issue()` 调用点**无需改动**（签名保持 `(string, time.Time, error)`）。

---

## Task 1: 引入 golang-jwt/jwt/v5 依赖

**Files:**
- Modify: `go.mod`, `go.sum`

- [ ] **Step 1: 添加依赖**

Run:
```bash
go get github.com/golang-jwt/jwt/v5@latest && go mod tidy
```
Expected: `go.mod` 的 `require` 块新增 `github.com/golang-jwt/jwt/v5 vX.X.X`，`go.sum` 出现对应条目。

- [ ] **Step 2: 验证可编译**

Run: `go build ./...`
Expected: 无报错（此时尚未使用该包，仅确认依赖可用）。

- [ ] **Step 3: 提交**

```bash
git add go.mod go.sum
git commit -m "$(cat <<'EOF'
chore(依赖): 引入 golang-jwt/jwt/v5 用于 JWT 认证

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 数据层 — Auth 模型与 store 访问器

**Files:**
- Modify: `internal/data/models.go`
- Modify: `internal/data/store.go`
- Test: `internal/data/store_test.go`

- [ ] **Step 1: 写失败测试（追加到 `store_test.go` 末尾，`assertErr` 之前）**

```go
func TestEnsureSecretGeneratesAndPersists(t *testing.T) {
	dir := t.TempDir()
	path := filepath.Join(dir, "homelab-panel.json")
	store, _, _ := Open(path, newTestLogger(t))

	if store.Secret() != "" {
		t.Fatal("secret should be empty before EnsureSecret")
	}
	if err := store.EnsureSecret(); err != nil {
		t.Fatalf("ensure: %v", err)
	}
	first := store.Secret()
	if first == "" {
		t.Fatal("secret should be generated after EnsureSecret")
	}
	// 幂等：再次调用不改变密钥
	if err := store.EnsureSecret(); err != nil {
		t.Fatalf("ensure again: %v", err)
	}
	if store.Secret() != first {
		t.Fatal("EnsureSecret should be idempotent")
	}
	// 持久化到磁盘
	reopened, _, _ := Open(path, newTestLogger(t))
	if reopened.Secret() != first {
		t.Fatal("secret should persist to disk")
	}
}

func TestIncrementTokenVersionPersists(t *testing.T) {
	dir := t.TempDir()
	path := filepath.Join(dir, "homelab-panel.json")
	store, _, _ := Open(path, newTestLogger(t))

	if v := store.TokenVersion(); v != 0 {
		t.Fatalf("initial version = %d, want 0", v)
	}
	if err := store.IncrementTokenVersion(); err != nil {
		t.Fatalf("increment: %v", err)
	}
	if v := store.TokenVersion(); v != 1 {
		t.Fatalf("version = %d, want 1", v)
	}
	reopened, _, _ := Open(path, newTestLogger(t))
	if v := reopened.TokenVersion(); v != 1 {
		t.Fatalf("persisted version = %d, want 1", v)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/data/ -run 'TestEnsureSecret|TestIncrementTokenVersion' -v`
Expected: 编译失败，提示 `store.Secret undefined` / `store.EnsureSecret undefined` / `store.TokenVersion undefined` / `store.IncrementTokenVersion undefined`。

- [ ] **Step 3: 在 `models.go` 增加 `Auth` 结构与字段**

在 `StoreData` 中 `Admin` 字段之后插入 `Auth` 字段，并在 `Admin` 结构定义之后新增 `Auth` 结构：

```go
type StoreData struct {
	Version   int       `json:"version"`
	Admin     Admin     `json:"admin"`
	Auth      Auth      `json:"auth"`
	Panel     Panel     `json:"panel"`
	Files     []File    `json:"files"`
	NextID    NextID    `json:"next_id"`
	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
}

type Admin struct {
	PasswordHash string    `json:"password_hash"`
	CreatedAt    time.Time `json:"created_at"`
	UpdatedAt    time.Time `json:"updated_at"`
}

// Auth 保存 JWT 签名密钥与 token 撤销版本号。
// 旧数据文件缺此段时为零值，由 Store.EnsureSecret 懒初始化（不 bump dataVersion）。
type Auth struct {
	Secret       string `json:"secret"`
	TokenVersion int    `json:"token_version"`
}
```

> 注意：`dataVersion` 保持 `2` 不变。

- [ ] **Step 4: 在 `store.go` 增加访问器**

在 `ResetPassword` 函数之后、`deepCopy` 之前插入：

```go
// Secret 返回 HS256 签名密钥（只读）。调用前应已通过 EnsureSecret 生成。
func (s *Store) Secret() string {
	s.mu.RLock()
	defer s.mu.RUnlock()
	return s.data.Auth.Secret
}

// TokenVersion 返回当前 token 撤销版本号。
func (s *Store) TokenVersion() int {
	s.mu.RLock()
	defer s.mu.RUnlock()
	return s.data.Auth.TokenVersion
}

// EnsureSecret 在密钥为空时生成 32 字节随机 hex 并持久化；已存在则原样保留（幂等）。
func (s *Store) EnsureSecret() error {
	return s.Save(func(d *StoreData) error {
		if d.Auth.Secret != "" {
			return nil
		}
		secret, err := generateSecret()
		if err != nil {
			return err
		}
		d.Auth.Secret = secret
		return nil
	})
}

// IncrementTokenVersion 递增撤销版本号，使所有已签发 token 失效（登出/改密时调用）。
func (s *Store) IncrementTokenVersion() error {
	return s.Save(func(d *StoreData) error {
		d.Auth.TokenVersion++
		return nil
	})
}

func generateSecret() (string, error) {
	var buf [32]byte
	if _, err := rand.Read(buf[:]); err != nil {
		return "", fmt.Errorf("read random: %w", err)
	}
	return hex.EncodeToString(buf[:]), nil
}
```

> `rand` 与 `hex` 已在 `store.go` 现有 import 中（见文件顶部 `crypto/rand`、`encoding/hex`），无需新增 import。

- [ ] **Step 5: 运行测试确认通过**

Run: `go test ./internal/data/ -v`
Expected: 全部通过，包括新增的两个测试。

- [ ] **Step 6: 提交**

```bash
git add internal/data/models.go internal/data/store.go internal/data/store_test.go
git commit -m "$(cat <<'EOF'
feat(数据层): 新增 Auth 段保存 JWT 密钥与 token 版本号

不 bump dataVersion，旧文件缺 auth 段时懒初始化，避免破坏性重置。

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 后端 JWT 迁移（原子改动，单一提交保证可编译）

> 本任务跨多个文件且互相耦合（`NewTokenManager` 签名变化会传导到 `handlers.go`；`Revoke` 签名变化传导到 `auth.go`；移除 `currentAdminToken` 影响 `middleware.go`/`auth.go`）。必须按顺序改完全部文件后再一起编译/测试/提交，保证提交点可编译。

**Files:**
- Rewrite: `internal/handlers/tokens.go`
- Rewrite: `internal/handlers/tokens_test.go`
- Modify: `internal/handlers/handlers.go`
- Modify: `internal/handlers/middleware.go`
- Modify: `internal/handlers/auth.go`
- Create: `internal/handlers/auth_test.go`

- [ ] **Step 1: 重写 `tokens.go` 为无状态 JWT 管理器**

整文件替换为：

```go
package handlers

import (
	"crypto/rand"
	"encoding/hex"
	"errors"
	"time"

	"homelab-panel/internal/data"

	"github.com/golang-jwt/jwt/v5"
)

// TokenManager 以无状态 JWT(HS256) 签发/校验管理员会话。
// 撤销通过 Store 中的 token 版本号实现，不持有任何 token 状态，因此跨进程重启仍有效。
type TokenManager struct {
	store *data.Store
	ttl   time.Duration
}

func NewTokenManager(store *data.Store, ttl time.Duration) *TokenManager {
	return &TokenManager{store: store, ttl: ttl}
}

type adminClaims struct {
	Version int `json:"ver"`
	jwt.RegisteredClaims
}

// Issue 签发 HS256 JWT，claims 携带当前 token 版本号；返回 token 与过期时间。
func (m *TokenManager) Issue() (string, time.Time, error) {
	if err := m.store.EnsureSecret(); err != nil {
		return "", time.Time{}, err
	}
	now := time.Now()
	expires := now.Add(m.ttl)
	var jtiBuf [16]byte
	if _, err := rand.Read(jtiBuf[:]); err != nil {
		return "", time.Time{}, err
	}
	claims := adminClaims{
		Version: m.store.TokenVersion(),
		RegisteredClaims: jwt.RegisteredClaims{
			Subject:   "admin",
			IssuedAt:  jwt.NewNumericDate(now),
			ExpiresAt: jwt.NewNumericDate(expires),
			ID:        hex.EncodeToString(jtiBuf[:]),
		},
	}
	token := jwt.NewWithClaims(jwt.SigningMethodHS256, claims)
	signed, err := token.SignedString([]byte(m.store.Secret()))
	if err != nil {
		return "", time.Time{}, err
	}
	return signed, expires, nil
}

// Valid 校验签名、过期与版本号是否匹配当前存储版本。
func (m *TokenManager) Valid(token string) bool {
	_, ok := m.validate(token)
	return ok
}

func (m *TokenManager) validate(token string) (adminClaims, bool) {
	secret := m.store.Secret()
	if token == "" || secret == "" {
		return adminClaims{}, false
	}
	var claims adminClaims
	parsed, err := jwt.ParseWithClaims(token, &claims, func(t *jwt.Token) (any, error) {
		if _, ok := t.Method.(*jwt.SigningMethodHMAC); !ok {
			return nil, errors.New("unexpected signing method")
		}
		return []byte(secret), nil
	})
	if err != nil || !parsed.Valid {
		return adminClaims{}, false
	}
	if claims.Version != m.store.TokenVersion() {
		return adminClaims{}, false
	}
	return claims, true
}

// Revoke 递增 token 版本号，使所有已签发 token 立即失效（登出/改密）。
func (m *TokenManager) Revoke() error {
	return m.store.IncrementTokenVersion()
}
```

- [ ] **Step 2: 改 `handlers.go` 构造器**

将 `NewHandler` 中的：

```go
		tokens:  NewTokenManager(7 * 24 * time.Hour),
```

改为：

```go
		tokens:  NewTokenManager(deps.Store, 30*24*time.Hour),
```

- [ ] **Step 3: 简化 `middleware.go`**

整文件替换为（移除已无用的 `adminTokenKey` / `currentAdminToken` / `c.Set`）：

```go
package handlers

import (
	"net/http"
	"strings"

	"github.com/gin-gonic/gin"
)

func (h *Handler) RequireAdmin() gin.HandlerFunc {
	return func(c *gin.Context) {
		token := bearerToken(c.GetHeader("Authorization"))
		if !h.tokens.Valid(token) {
			writeError(c, http.StatusUnauthorized, "invalid or missing admin token")
			c.Abort()
			return
		}
		c.Next()
	}
}

func bearerToken(header string) string {
	kind, token, ok := strings.Cut(header, " ")
	if !ok || !strings.EqualFold(kind, "Bearer") {
		return ""
	}
	return strings.TrimSpace(token)
}
```

- [ ] **Step 4: 改 `auth.go` — 登出与改密**

将 `DeleteAdminSession` 与 `UpdateAdminPassword` 替换为（`CreateAdminSession` 保持不变）：

```go
func (h *Handler) DeleteAdminSession(c *gin.Context) {
	if err := h.tokens.Revoke(); err != nil {
		writeError(c, http.StatusInternalServerError, "revoke session failed")
		return
	}
	c.Status(http.StatusNoContent)
}

func (h *Handler) UpdateAdminPassword(c *gin.Context) {
	var req passwordRequest
	if err := c.ShouldBindJSON(&req); err != nil {
		writeError(c, http.StatusBadRequest, "invalid request body")
		return
	}
	if req.NewPassword == "" {
		writeError(c, http.StatusBadRequest, "newPassword is required")
		return
	}
	if len(req.NewPassword) < 6 {
		writeError(c, http.StatusBadRequest, "newPassword must be at least 6 characters")
		return
	}
	err := h.Store.UpdatePassword(req.OldPassword, req.NewPassword)
	if err != nil {
		if errors.Is(err, data.ErrInvalidPassword) {
			writeError(c, http.StatusUnauthorized, "old password is incorrect")
			return
		}
		writeError(c, http.StatusInternalServerError, "update password failed")
		return
	}
	// 改密成功：作废旧 token（踢掉其他设备），并为当前会话重签
	if err := h.tokens.Revoke(); err != nil {
		writeError(c, http.StatusInternalServerError, "rotate session failed")
		return
	}
	token, expires, err := h.tokens.Issue()
	if err != nil {
		writeError(c, http.StatusInternalServerError, "reissue session failed")
		return
	}
	writeJSON(c, http.StatusOK, gin.H{"ok": true, "token": token, "expires_at": expires})
}
```

- [ ] **Step 5: 重写 `tokens_test.go`**

整文件替换为：

```go
package handlers

import (
	"path/filepath"
	"testing"
	"time"

	"homelab-panel/internal/data"

	"github.com/golang-jwt/jwt/v5"
	"go.uber.org/zap"
)

func newTokenTestStore(t *testing.T) *data.Store {
	t.Helper()
	logger, _ := zap.NewDevelopment()
	s, _, err := data.Open(filepath.Join(t.TempDir(), "homelab-panel.json"), logger)
	if err != nil {
		t.Fatalf("open store: %v", err)
	}
	if err := s.EnsureSecret(); err != nil {
		t.Fatalf("ensure secret: %v", err)
	}
	return s
}

func TestTokenIssueAndValidate(t *testing.T) {
	m := NewTokenManager(newTokenTestStore(t), time.Hour)
	token, _, err := m.Issue()
	if err != nil {
		t.Fatalf("issue: %v", err)
	}
	if token == "" || !m.Valid(token) {
		t.Fatal("issued token should be valid")
	}
	// 应为可解析的 JWT，而非 opaque 字符串
	if _, err := jwt.ParseUnverified(token, &adminClaims{}); err != nil {
		t.Fatalf("token should be a parseable JWT: %v", err)
	}
}

func TestTokenExpiredInvalid(t *testing.T) {
	m := NewTokenManager(newTokenTestStore(t), 1*time.Millisecond)
	token, _, _ := m.Issue()
	time.Sleep(20 * time.Millisecond)
	if m.Valid(token) {
		t.Fatal("expired token should be invalid")
	}
}

func TestTokenTamperedInvalid(t *testing.T) {
	m := NewTokenManager(newTokenTestStore(t), time.Hour)
	token, _, _ := m.Issue()
	// 篡改末位字符，破坏签名
	tail := token[len(token)-1:]
	if tail == "A" {
		token = token[:len(token)-1] + "B"
	} else {
		token = token[:len(token)-1] + "A"
	}
	if m.Valid(token) {
		t.Fatal("tampered token should be invalid")
	}
}

func TestTokenEmptyInvalid(t *testing.T) {
	m := NewTokenManager(newTokenTestStore(t), time.Hour)
	if m.Valid("") {
		t.Fatal("empty token should be invalid")
	}
}

func TestRevokeInvalidatesIssued(t *testing.T) {
	store := newTokenTestStore(t)
	m := NewTokenManager(store, time.Hour)
	token, _, _ := m.Issue()
	if !m.Valid(token) {
		t.Fatal("token should be valid before revoke")
	}
	if err := m.Revoke(); err != nil {
		t.Fatalf("revoke: %v", err)
	}
	if m.Valid(token) {
		t.Fatal("revoked token should be invalid")
	}
	// 版本号已递增：重签的新 token 可用，旧 token 仍失效
	token2, _, _ := m.Issue()
	if !m.Valid(token2) {
		t.Fatal("new token after revoke should be valid")
	}
	if m.Valid(token) {
		t.Fatal("old token should remain invalid after re-issue")
	}
}
```

- [ ] **Step 6: 新建 `auth_test.go` — handler 行为测试**

```go
package handlers

import (
	"bytes"
	"encoding/json"
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/gin-gonic/gin"
)

// newAuthHandler 返回已就绪的 handler 与已知管理员密码（便于测试登录）。
func newAuthHandler(t *testing.T) (*Handler, string) {
	t.Helper()
	gin.SetMode(gin.TestMode)
	store := mustStore(t)
	if err := store.EnsureSecret(); err != nil {
		t.Fatalf("ensure secret: %v", err)
	}
	const password = "secret123"
	if err := store.ResetPassword(password); err != nil {
		t.Fatalf("reset password: %v", err)
	}
	h := NewHandler(Deps{Store: store, DataDir: t.TempDir()})
	return h, password
}

func TestCreateSessionIssuesValidJWT(t *testing.T) {
	h, password := newAuthHandler(t)
	r := gin.New()
	r.POST("/api/v1/admin/session", h.CreateAdminSession)

	body, _ := json.Marshal(map[string]string{"password": password})
	req := httptest.NewRequest(http.MethodPost, "/api/v1/admin/session", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	w := httptest.NewRecorder()
	r.ServeHTTP(w, req)

	if w.Code != http.StatusCreated {
		t.Fatalf("expected 201, got %d (body=%s)", w.Code, w.Body.String())
	}
	var resp struct {
		Token string `json:"token"`
	}
	if err := json.Unmarshal(w.Body.Bytes(), &resp); err != nil {
		t.Fatalf("unmarshal: %v", err)
	}
	if !h.tokens.Valid(resp.Token) {
		t.Fatal("issued token should validate")
	}
}

func TestCreateSessionRejectsWrongPassword(t *testing.T) {
	h, _ := newAuthHandler(t)
	r := gin.New()
	r.POST("/api/v1/admin/session", h.CreateAdminSession)

	body, _ := json.Marshal(map[string]string{"password": "wrong"})
	req := httptest.NewRequest(http.MethodPost, "/api/v1/admin/session", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	w := httptest.NewRecorder()
	r.ServeHTTP(w, req)
	if w.Code != http.StatusUnauthorized {
		t.Fatalf("expected 401, got %d", w.Code)
	}
}

func TestLogoutRevokesSession(t *testing.T) {
	h, _ := newAuthHandler(t)
	token, _, _ := h.tokens.Issue()
	if !h.tokens.Valid(token) {
		t.Fatal("token should be valid before logout")
	}
	r := gin.New()
	r.DELETE("/api/v1/admin/session", h.RequireAdmin(), h.DeleteAdminSession)

	req := httptest.NewRequest(http.MethodDelete, "/api/v1/admin/session", nil)
	req.Header.Set("Authorization", "Bearer "+token)
	w := httptest.NewRecorder()
	r.ServeHTTP(w, req)
	if w.Code != http.StatusNoContent {
		t.Fatalf("expected 204, got %d", w.Code)
	}
	if h.tokens.Valid(token) {
		t.Fatal("token should be invalid after logout")
	}
}

func TestChangePasswordReissuesToken(t *testing.T) {
	h, password := newAuthHandler(t)
	token, _, _ := h.tokens.Issue()

	r := gin.New()
	r.PUT("/api/v1/admin/password", h.RequireAdmin(), h.UpdateAdminPassword)

	body, _ := json.Marshal(map[string]string{
		"old_password": password,
		"new_password": "newpass456",
	})
	req := httptest.NewRequest(http.MethodPut, "/api/v1/admin/password", bytes.NewReader(body))
	req.Header.Set("Authorization", "Bearer "+token)
	req.Header.Set("Content-Type", "application/json")
	w := httptest.NewRecorder()
	r.ServeHTTP(w, req)
	if w.Code != http.StatusOK {
		t.Fatalf("expected 200, got %d (body=%s)", w.Code, w.Body.String())
	}
	var resp struct {
		OK    bool   `json:"ok"`
		Token string `json:"token"`
	}
	if err := json.Unmarshal(w.Body.Bytes(), &resp); err != nil {
		t.Fatalf("unmarshal: %v", err)
	}
	if !resp.OK || resp.Token == "" {
		t.Fatalf("expected ok+token, got %+v", resp)
	}
	if h.tokens.Valid(token) {
		t.Fatal("old token should be invalid after password change")
	}
	if !h.tokens.Valid(resp.Token) {
		t.Fatal("reissued token should be valid")
	}
}
```

- [ ] **Step 7: 编译并运行全部后端测试**

Run: `go build ./... && go test ./...`
Expected: 编译通过；`internal/data`、`internal/handlers`（含 `panel_test.go`、`tokens_test.go`、`auth_test.go`、`wire_test.go`）全部通过。
> `panel_test.go` 中的 `h.tokens.Issue()` 无需改动 —— `Issue()` 签名未变且内部会 `EnsureSecret`。

- [ ] **Step 8: 提交**

```bash
git add internal/handlers/tokens.go internal/handlers/tokens_test.go \
        internal/handlers/handlers.go internal/handlers/middleware.go \
        internal/handlers/auth.go internal/handlers/auth_test.go
git commit -m "$(cat <<'EOF'
feat(认证): 用 JWT(HS256)+版本号撤销替换内存 TokenManager

- TokenManager 改为无状态 JWT 签发/校验，登录态跨重启存活
- 登出/改密通过递增 token 版本号作废所有旧 token
- 改密后为当前会话重签 token，保持登录、踢掉其他设备
- 移除已无用的 token 上下文辅助代码

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 启动时确保密钥已生成（生产保证）

**Files:**
- Modify: `internal/app/app.go`

> `TokenManager.Issue()` 已会 `EnsureSecret`，但启动时显式生成可保证：即便无人登录，`Validate` 路径的密钥也已就绪，且首登前无写竞争。

- [ ] **Step 1: 在 `app.go` 的 `data.Open` 成功后追加密钥初始化**

将：

```go
	store, password, err := data.Open(filepath.Join(dataDir, "homelab-panel.json"), logger)
	if err != nil {
		return nil, err
	}
```

改为：

```go
	store, password, err := data.Open(filepath.Join(dataDir, "homelab-panel.json"), logger)
	if err != nil {
		return nil, err
	}
	if err := store.EnsureSecret(); err != nil {
		return nil, fmt.Errorf("ensure auth secret: %w", err)
	}
```

- [ ] **Step 2: 编译验证**

Run: `go build ./...`
Expected: 通过。

- [ ] **Step 3: 提交**

```bash
git add internal/app/app.go
git commit -m "$(cat <<'EOF'
feat(启动): 服务启动时确保 JWT 密钥已生成

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 前端 — 改密响应类型更新

**Files:**
- Modify: `web/src/api/admin.ts`

> 现状：`changePassword` 仅有定义、无调用方（前端尚无改密 UI）。后端改密响应已新增 `token` / `expires_at`，此处仅同步类型。当未来新增改密 UI 时，调用方应在成功后执行 `useAuthStore.getState().setToken(res.data.token)`（若存在）以无缝续会。

- [ ] **Step 1: 更新 `changePassword` 返回类型**

将：

```ts
export function changePassword(oldPassword: string, newPassword: string) {
    return put<{ ok: boolean }>({
        url: '/admin/password',
        data: { old_password: oldPassword, new_password: newPassword },
    })
}
```

改为：

```ts
export function changePassword(oldPassword: string, newPassword: string) {
    return put<{ ok: boolean; token?: string; expires_at?: string }>({
        url: '/admin/password',
        data: { old_password: oldPassword, new_password: newPassword },
    })
}
```

- [ ] **Step 2: 类型检查与测试**

Run: `cd web && npm run type-check && npm run test`
Expected: 类型检查通过；现有 vitest 用例全部通过（无 admin 相关测试受影响）。

- [ ] **Step 3: 提交**

```bash
git add web/src/api/admin.ts
git commit -m "$(cat <<'EOF'
feat(前端): changePassword 响应类型补充 token/expires_at

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: 全量校验 + 重启存活验收

**Files:** 无（验证步骤）

- [ ] **Step 1: 后端全量静态检查与测试**

Run: `go vet ./... && go test ./...`
Expected: 无 vet 告警；全部测试通过。

- [ ] **Step 2: 构建完整产物**

Run: `make all`
Expected: 前端构建（`npm run build`）+ 后端编译均成功，生成 `homelab-panel` 二进制。

- [ ] **Step 3: 重启存活验收（核心目标）**

用临时数据目录跑两次，验证 token 跨重启有效。用 `reset-password` 设置已知密码（首启密码打到 stdout 不便抓取）：

```bash
DEMO=$(mktemp -d)
./homelab-panel reset-password --dir "$DEMO" --password demo123456

# 第一次启动并登录拿 token
./homelab-panel serve --dir "$DEMO" --port 3002 >/tmp/hp1.log 2>&1 &
SERVER_PID=$!
sleep 1
TOKEN=$(curl -s -X POST http://localhost:3002/api/v1/admin/session \
    -H 'Content-Type: application/json' \
    -d '{"password":"demo123456"}' | sed -E 's/.*"token":"([^"]+)".*/\1/')
[ -n "$TOKEN" ] || { echo "登录失败，日志："; cat /tmp/hp1.log; kill $SERVER_PID; exit 1; }

# 用 token 访问受保护接口（重启前）
curl -s -o /dev/null -w "重启前: %{http_code}\n" http://localhost:3002/api/v1/files \
    -H "Authorization: Bearer $TOKEN"
# 预期: 重启前: 200

# 重启服务
kill $SERVER_PID; sleep 1
./homelab-panel serve --dir "$DEMO" --port 3002 >/tmp/hp2.log 2>&1 &
SERVER_PID=$!
sleep 1
# 同一个 token 重启后仍应有效
curl -s -o /dev/null -w "重启后: %{http_code}\n" http://localhost:3002/api/v1/files \
    -H "Authorization: Bearer $TOKEN"
# 预期: 重启后: 200（重启后不再需要重新登录 ← 核心目标）

kill $SERVER_PID
rm -rf "$DEMO"
```
Expected: 输出 `重启前: 200` 与 `重启后: 200`。

- [ ] **Step 4: 撤销验收（登出使旧 token 失效）**

单独起一个会话验证登出作废旧 token：

```bash
DEMO=$(mktemp -d)
./homelab-panel reset-password --dir "$DEMO" --password demo123456
./homelab-panel serve --dir "$DEMO" --port 3002 >/tmp/hp3.log 2>&1 &
SERVER_PID=$!
sleep 1
TOKEN=$(curl -s -X POST http://localhost:3002/api/v1/admin/session \
    -H 'Content-Type: application/json' \
    -d '{"password":"demo123456"}' | sed -E 's/.*"token":"([^"]+)".*/\1/')

curl -s -o /dev/null -w "登出: %{http_code}\n" -X DELETE http://localhost:3002/api/v1/admin/session \
    -H "Authorization: Bearer $TOKEN"
# 预期: 登出: 204
curl -s -o /dev/null -w "登出后再访问: %{http_code}\n" http://localhost:3002/api/v1/files \
    -H "Authorization: Bearer $TOKEN"
# 预期: 登出后再访问: 401（版本号已递增，旧 token 失效）

kill $SERVER_PID
rm -rf "$DEMO"
```
Expected: 输出 `登出: 204` 与 `登出后再访问: 401`。

---

## 完成

至此：登录态以 JWT 形式跨重启存活，登出/改密通过版本号撤销，密钥与版本号持久化在 `data/homelab-panel.json` 的 `auth` 段（未 bump 数据版本）。
